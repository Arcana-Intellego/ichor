#!/usr/bin/env bash
set -euo pipefail

script_source="${BASH_SOURCE[0]}"
script_parent="${script_source%/*}"
[[ "${script_parent}" != "${script_source}" ]] || script_parent="."
script_dir="$(cd "${script_parent}" && pwd)"
exec bash "${script_dir}/install_ichor.sh" --machine csf3 "$@"
